schema: ./types.graphql
generates:
  ./src/graphql/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./dist/src/graphql/types.ts:
    plugins:
      - typescript
      - typescript-resolvers